version: "3.7"
services:

    easypost-ui:
        build: .
        restart: always
        container_name: easypost-ui
        volumes: 
            - ./src:/var/www/html
        ports:
            - "8000:80"
        networks:
            - easypost-ui

networks:
    easypost-ui:
        name: easypost-ui
